Make sure you have the PYgame library installed
to install open youre terminal an type in:

Windows:
pip install pygame

Linux:
pip3 install pygame

MacOS:
pip install pygame

also to mention to make sure you have python installed or actualizated
